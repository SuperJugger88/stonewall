apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: default
spec:
  ports:
  - name: http
    port: 4000
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/instance: api
    app.kubernetes.io/name: api
  type: ClusterIP
